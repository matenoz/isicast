<section ng-controller="ClassesController" ng-init="findOne()">
  <div class="page-header">
    <h1 ng-bind="classe.title"></h1>
  </div>
  <div class="pull-right" ng-show="authentication.user._id == classe.user._id">
    <a class="btn btn-primary" ui-sref="classes.edit({classeId: classe._id})">
      <i class="glyphicon glyphicon-edit"></i>
    </a>
    <a class="btn btn-primary" ng-click="remove();">
      <i class="glyphicon glyphicon-trash"></i>
    </a>
  </div>
  <small>
    <em class="text-muted">
      Posted on
      <span ng-bind="classe.created | date:'mediumDate'"></span>
      by
      <span ng-bind="classe.user.displayName"></span>
    </em>
  </small>
  <p class="lead" ng-bind="classe.content"></p>
</section>
