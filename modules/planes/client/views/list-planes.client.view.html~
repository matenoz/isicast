<section ng-controller="PlanesController" ng-init="find()">
  <div class="page-header">
    <h1>Planes</h1>
  </div>
  <div class="list-group">
    <a ng-repeat="plane in planes" ui-sref="planes.view({planeId: plane._id})" class="list-group-item">
      <small class="list-group-item-text">
        Posted on
        <span ng-bind="plane.created | date:'mediumDate'"></span>
        by
        <span ng-bind="plane.user.displayName"></span>
      </small>
      <h4 class="list-group-item-heading" ng-bind="plane.title"></h4>
      <p class="list-group-item-text" ng-bind="plane.content"></p>
    </a>
  </div>
  <div class="alert alert-warning text-center" ng-if="planes.$resolved && !planes.length">
    No planes yet, why don't you <a ui-sref="planes.create">create one</a>?
  </div>
</section>
