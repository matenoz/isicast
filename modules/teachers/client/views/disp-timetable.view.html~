<section ng-controller="TimetablesController" ng-init="find()">
  <div class="page-header">
    <h3>Quadro settimanale disposizioni</h3>
  </div>
  <div class="container">
     <div class="row">
       <button type="button" class="btn btn-xs btn-flat" ng-click="avail_hours()"> Disposizioni docenti</button>
    <hr/>
          <table  class="table table-bordered" style="table-layout:fluid;">
	    <thead > 
	      <tr>
		<th>ora</th>
		<th>lunedi</th>
		<th>martedi</th>
		<th>mercoledi</th>
		<th>giovedi</th>
		<th>venerdi</th>
	    </tr>
	    </thead>
	    <tbody>
	      <tr ng-repeat="disp in newdisp">
		<td ><p>{{disp.nome_ora}}</p></td>
		<td ><p>{{disp.lunedi.join(", ")}}</p></td>
		<td ><p>{{disp.martedi.join(", ")}}</p></td>
		<td ><p>{{disp.mercoledi.join(", ")}}</p></td>
		<td ><p>{{disp.giovedi.join(", ")}}</p></td>
		<td ><p>{{disp.venerdi.join(", ")}}</p></td>
	      </tr>
	    </tbody>
	  </table>
       </div>
   </div>
</section>
